# AI Prompt Record

The following prompts summarize how the AI tool was directed during this assignment.

1. "Find a recent deep CNN algorithm suitable for a computer vision assignment. The model must be simple enough for MNIST and an 8 GB GPU."
2. "Adapt ConvNeXt to 28 x 28 grayscale MNIST images. Reduce the channel widths and number of blocks, but retain depthwise convolution, LayerNorm, pointwise expansion, GELU, and residual connections."
3. "Implement the adapted model in PyTorch. Use 10,000 training images and 2,000 test images, save metrics and figures, and keep the project easy to reproduce."
4. "Choose modest experiment settings that prioritize assignment completion: three epochs, batch size 64, AdamW, and deterministic data subsets."
5. "Write an assignment report covering AI-assisted algorithm search, algorithm description, AI implementation, experiment settings and results, lessons learned, and the source-code webpage."

These prompts document the AI-assisted workflow required by the assignment. The final report uses the actual results produced by running `main.py`.
